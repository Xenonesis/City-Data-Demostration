{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "vercel-build": "npm run build && composer install --no-dev --optimize-autoloader --no-interaction"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.20",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.1",
        "postcss": "^8.4.49",
        "tailwindcss": "^3.4.17",
        "vite": "^7.0.4"
    }
}
